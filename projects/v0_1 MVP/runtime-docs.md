# Cortex v0.1 MVP Runtime Docs

## 1. Purpose

Capture the operator and demo walkthrough for the current MVP execution package.

## 2. Intended Walkthrough

1. bring up required dependencies
2. import or register a small starter artifact set
3. confirm knowledge extraction and storage behavior
4. run retrieval against that stored knowledge
5. run a quick capture that reinforces or extends the corpus
6. ask a chat question that should improve because Cortex retrieval runs first
7. inspect the resulting bundle and trace surfaces

## 3. Current Note

This file is the project-level runtime walkthrough placeholder. It should be refined as the implementation thread produces real commands, evidence, and operator notes.
