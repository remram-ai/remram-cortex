# 2026-04-03 Layered Repository Reset

This bucket contains the material that was active before the repository was refactored around the layered architecture and chosen stack.

It includes:

- older `docs/design` decision packages and alternative stack explorations
- the old `docs/reference` corpus
- the redundant `docs/remram-cortex` subtree
- archived overview material that previously lived under `docs/overview/archive`
- the old `product/` tree
- the old `projects/` tree, including the original `v0_1 MVP`

Nothing in this bucket is deleted.

It is preserved for:

- historical context
- decision traceability
- later salvage of ideas that no longer belong in the active working tree
