# Epic 02: High-Signal Mamba Stream

This epic is deferred by default to Phase 3.

It may be pulled forward as a Phase `1.5` spike if the post-Phase-1 decision gate shows that local continuity pressure is already a real problem.

Its job is to add the always-on narrow high-signal listener later without changing the Phase 1 core spine.
